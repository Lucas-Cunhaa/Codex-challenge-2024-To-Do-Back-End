var jsondiffpatch = require('jsondiffpatch'),
    EventEmitter = require('events').EventEmitter;


/**
 * UndoStore - Construct a new UndoStore based off the supplied json object
 *
 * @param  {object|array} json Initial state of the store
 */
function UndoStore(json){
    this.state = 
}

// Inherits from EventEmitter
UndoStore.prototype = Object.create(EventEmitter.prototype);
